Вопросы к этому заданию

1.Сколько типов данных существует в JS?

2.Как вывести информацию в консоль?

3.Какая функция операторов || и &&?

1.существует 7 типов данных:

-строки,
-числа,
-null,
-undefined,
-object{array},
-symbol,
-boolen

2. С помощью команді console.log();

3. Проверяют состояние елемента на булевое занчение
правда, ложь.

|| - true , если  хотя бы  одно из состояний правда или два;
&& - true, только когда два true 